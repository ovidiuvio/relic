apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
data:
  config.yaml: |
    ingress:
      - hostname: HOSTNAME_PLACEHOLDER
        service: http://frontend:80
      - service: http_status:404
